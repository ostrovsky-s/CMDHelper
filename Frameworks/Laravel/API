Dont deasign API such as your database

install api auth

composer require laravel/passport   //then
artisan migrate
========================================================= WEB TYPES OF API
SOAP XML-RPC JSON-RPC REST


laravel api drivers: token, passport
========================================================= Main commands for create API
php artisan preset none   	it will leave only an SASS file and some common JavaScript utility libraries
php artisan route:list  	list of routers

